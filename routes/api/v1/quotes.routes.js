// example: quotes.routes.js
const express = require('express');
const router = express.Router();

// Define routes here
// router.get('/', (req, res) => res.send('Quotes endpoint'));

module.exports = router; // ✅ must export the router